# Правки заказчика от 13.08.2026

Источник: `Правки Joim-Store.docx`. Картинки из него разобраны
в `inbox/pravki-2026-08-13/` в порядке появления в документе.

## Что просили и что сделано

| Правка | Состояние |
| --- | --- |
| Фирменные цвета точными HEX | ✅ внесены |
| Шрифт Panton Bold | ⛔ нужны файлы шрифта |
| Звезда как фирменный элемент | ⛔ нужен вектор из брендбука |
| Чередование цветов по блокам | ✅ светлый блок в тёмной странице |
| «Сочные» товары с тенями и объёмом | ✅ частично: карточки уже с наездом и вторым ракурсом |
| «Как делать НЕ НАДО» | ✅ кадр заменён |

## Палитра

Взята из документа целиком:

| Название | HEX | Где на сайте |
| --- | --- | --- |
| JOIM Green | `#99C453` | акцент |
| Graphite Site | `#111313` | фон страницы |
| Graphite Dark | `#1D211F` | приподнятая плашка |
| Graphite Mid | `#3A3D39` | рамки кадров |
| White | `#F8FAFC` | основной текст |
| Muted Gray | `#AEB6B1` | вторичный текст |
| Apple Light Gray | `#F5F5F7` | светлые блоки |
| Pure White | `#FFFFFF` | плитки в светлых блоках |
| Black | `#000000` | в интерфейсе не используется |

Прежний зелёный был `#8CC53F` — снят пипеткой с логотипа и на полтона
холоднее фирменного.

Две ступени пришлось вывести самим: `surface-2` между Dark и Mid
(в палитре ступени нет, а сетке она нужна) и третья серая для мелочи
11–13 px. Обе проверены на контраст: на всех тёмных поверхностях
не ниже 4,5:1.

## Чередование блоков

Референс — Dyson и собственный макет заказчика: тёмная страница,
светлая плашка категорий.

Сделано классом `.section-light`, который переопределяет токены внутри
своей области. Ни один компонент не переписан: утилиты Tailwind ссылаются
на те же переменные, секция ставит класс — и весь её текст, линии
и плашки переворачиваются на светлое.

**Зелёный внутри светлого блока затемнён до `#4D6D22`.** Фирменный
`#99C453` на `#F5F5F7` даёт 1,86:1 — текстом он там быть не может.
Заливкой остаётся прежним: тёмные буквы на нём читаются с запасом 8:1,
как на макете заказчика с зелёными кнопками.

Светлым сделан блок категорий — ровно та плашка, что светлая на макете.

## «Как делать НЕ НАДО»

Заказчик обвёл на скриншоте нашего сайта кадры, где устройство лежит
наклейкой соответствия к камере: страна-производитель, значки
сертификации. В витрине это читается как коробка.

Кадры `joim-es19-1` и `joim-es29-1` — парные: одно устройство стоит,
второе лежит наклейкой вверх. Стоящее вырезано в отдельные файлы
`joim-es19-front` и `joim-es29-front`, они стали первыми в галереях.
Парные кадры из галерей убраны.

Кадр наведения остался тем, что просили: ES-19 — порты зарядки,
ES-29 — в сборе с клеммами, PVC-1 — кейс с насадками.

## Чего не сделано и почему

**Шрифт Panton Bold.** Коммерческая гарнитура Fontfabric, файлов
в проекте нет и купить их за заказчика нельзя. Нужны `.woff2`
с лицензией на веб. Пока стоит Geist — до замены он ближайший
по рисунку из открытых.

**Звезда.** На сайте сейчас выдуманный знак «J», и это ошибка:
у бренда есть свой знак, он на каждом устройстве. Перерисовывать его
по фотографии не стали — приблизительная копия фирменного знака хуже
его отсутствия. Нужен вектор из брендбука (`.svg` или `.ai`).

После вектора звезда идёт в четыре места, как в примерах заказчика:
знак в шапке вместо «J», маркеры списков, полупрозрачная подложка
в крупных блоках и фоновые линии.

**Фоновая сетка ждёт того же вектора.** Сейчас под первым экраном,
кадрами товара и анкетой лежит утилита `grid-lines` — универсальная
сетка 88×88 из двух градиентов. Заказчик просит другого: «тонкие
зелёные полоски в форме сторон логотипа-звезды» и «полупрозрачные
фоновые элементы также в виде полосок логотипа». Сетка держит место
до замены: убрать её сейчас — оставить фон пустым, а нарисовать
фирменные линии не из чего.
